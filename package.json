{
  "name": "darukaa-earth",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "test": "npm --prefix frontend run test",
    "prepare": "husky"
  },
  "lint-staged": {
    "frontend/src/**/*.{js,jsx}": "prettier --write",
    "backend/**/*.py": "ruff format"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3"
  }
}
